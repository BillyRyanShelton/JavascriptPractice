function getElementsLessThan100AtProperty(obj, key) {
  if(!obj.hasOwnProperty(key) || !Array.isArray(obj[key]) || obj[key] === [])  { 
    return []; 
  } return obj[key].filter(function(element) { return element < 100; })
}
