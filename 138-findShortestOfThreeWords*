function findShortestOfThreeWords(word1, word2, word3) {
  return [word1, word2, word2].reduce(function(shortest, element) {
    return (shortest.length > element.length) ? element : shortest;
  })
}
