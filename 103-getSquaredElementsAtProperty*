function getSquaredElementsAtProperty(obj, key) {
  if(!obj.hasOwnProperty(key) || !Array.isArray(obj[key]) || obj[key] === 0) {
    return [];
  } return obj[key].map(function(element) { return element * element;})
}
