function getLengthOfLongestElement(arr) {
  if(arr.length === 0) {
    return 0;
  }  var longestNum = arr[0].length;
  for(var i = 0; i < arr.length; i++) {
    if(longestNum < arr[i].length) { 
      longestNum = arr[i].length;
    }
  } return longestNum;
}
