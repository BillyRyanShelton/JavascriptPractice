function getElementOfArrayProperty(obj, key, index) {
  if(!obj.hasOwnProperty(key) || !Array.isArray(obj[key]) ||  (obj[key].length === 0) || (obj[key].length < index + 1)) {
    return undefined;
  } return obj[key][index];
}
