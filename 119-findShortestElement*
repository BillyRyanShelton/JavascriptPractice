function findShortestElement(arr) {
  if(arr.length === 0) {
    return '';
  }
  return arr.reduce(function(shortest, element) {
    if(shortest.length > element.length) {
      return element;
    } return shortest;
  })
}
